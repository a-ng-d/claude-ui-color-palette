{
  "skill_name": "figma-variables-tokens-generator",
  "evals": [
    {
      "id": 1,
      "prompt": "I want to create a Figma design token system for my B2B SaaS dashboard.",
      "expected_output": "Skill starts at Turn 1 with a dropdown asking about existing Figma system. Proceeds sequentially through all turns (1–10) — each turn in a separate message. Does not skip turns or bundle multiple turns in one message. Asks dropdowns via ask_user_input tool calls. Uses any context clues (e.g. 'SaaS dashboard') to inject Rec[value] suggestions into questions but still asks them. After all turns are complete and user confirms the manifest, produces a ZIP file with correctly structured JSON. Token count table delivered after ZIP.",
      "files": [],
      "assertions": [
        {
          "name": "starts-with-turn-1-dropdown",
          "description": "First response uses ask_user_input for Turn 1 — not a freeform question and not Turn 2 or later"
        },
        {
          "name": "turns-are-sequential-not-bundled",
          "description": "Turn 2 is not shown in the same message as Turn 1"
        },
        {
          "name": "dropdowns-use-ask-user-input-tool",
          "description": "Questions labeled ask_user_input are sent as real tool calls, not as freeform prose"
        },
        {
          "name": "zip-delivered-not-raw-json",
          "description": "Final output is a downloadable ZIP file, not raw JSON dumped in the chat"
        },
        {
          "name": "token-count-table-present",
          "description": "A token count table is delivered after the ZIP with per-collection counts"
        }
      ]
    },
    {
      "id": 2,
      "prompt": "Help me build a Figma token system for a mobile app.",
      "expected_output": "Skill starts at Turn 1 with a dropdown. Runs all questionnaire turns sequentially. Uses 'mobile app' context to inject Rec[value] suggestions (e.g. recommending mobile architecture, suggesting dark mode) but still asks every question via dropdowns — does not skip or auto-decide. After user works through all turns and confirms manifest, generates a ZIP appropriate for the architecture confirmed during the questionnaire. Token count table delivered.",
      "files": [],
      "assertions": [
        {
          "name": "starts-with-turn-1-dropdown",
          "description": "First response uses ask_user_input for Turn 1 — not a freeform question"
        },
        {
          "name": "rec-value-injected-from-context",
          "description": "Mobile app context is used to inject a [Rec: value] suggestion in at least one dropdown question (e.g. product type or architecture)"
        },
        {
          "name": "architecture-question-still-asked",
          "description": "Q7 (Tier architecture) is asked via dropdown even though the context implies mobile — not auto-decided"
        },
        {
          "name": "zip-delivered-not-raw-json",
          "description": "Final output is a downloadable ZIP, not raw JSON in the chat"
        },
        {
          "name": "token-count-table-present",
          "description": "A token count table is delivered after the ZIP"
        }
      ]
    },
    {
      "id": 3,
      "prompt": "I need to build an enterprise design system for our fintech platform. We already have a Figma file with some variables I can export.",
      "expected_output": "Skill starts at Turn 1 and, because the user has an existing Figma system, provides the export instructions for exporting variables from Figma. Waits for the uploaded files before proceeding to Turn 2. After receiving files, analyses them and adapts subsequent questions to include 'Keep existing' options alongside expansion options. Runs all turns sequentially. After user confirms manifest, generates a ZIP with correct structure for the confirmed architecture. Token count table delivered after ZIP.",
      "files": [],
      "assertions": [
        {
          "name": "turn-1-provides-export-instructions",
          "description": "When user indicates existing Figma system, Turn 1 response includes the step-by-step Figma variable export instructions"
        },
        {
          "name": "waits-for-uploaded-files",
          "description": "Turn 1 stops and waits for uploaded files — does not proceed to Turn 2 in the same message"
        },
        {
          "name": "keep-existing-options-injected",
          "description": "After file analysis, at least one dropdown includes a 'Keep existing:' option reflecting the uploaded system's conventions"
        },
        {
          "name": "zip-delivered-not-raw-json",
          "description": "Final output is a downloadable ZIP, not raw JSON in the chat"
        },
        {
          "name": "token-count-table-present",
          "description": "A token count table is delivered after the ZIP"
        }
      ]
    },
    {
      "id": 4,
      "prompt": "I already have variables in Figma and want the AI to analyze them before generating updates.",
      "expected_output": "Turn 1 instructs the user to export existing variables using the Variables Tokens Collections Importer plugin Export tab and share the ZIP or file path. It does not instruct the user to manually right-click collections and export them one by one.",
      "files": [],
      "assertions": [
        {
          "name": "uses-plugin-export-flow-in-turn-1",
          "description": "Turn 1 explicitly points the user to the Variables Tokens Collections Importer plugin Export tab"
        },
        {
          "name": "does-not-use-manual-right-click-export-flow",
          "description": "Turn 1 does not instruct the user to right-click each Figma collection and export variables manually"
        }
      ]
    },
    {
      "id": 5,
      "prompt": "Generate a complex 4-tier token package locally in a coding workspace.",
      "expected_output": "When operating in a local IDE or CLI workspace, the final deliverable is a ZIP inside an exports folder. The skill does not scatter helper manifests or debug artifacts in the project root unless explicitly requested.",
      "files": [],
      "assertions": [
        {
          "name": "writes-final-zip-to-exports-folder",
          "description": "Local workspace output goes to an exports folder, not the project root"
        },
        {
          "name": "avoids-extra-artifacts-by-default",
          "description": "The default deliverable is the ZIP only; manifests/debug files are not emitted unless explicitly requested"
        }
      ]
    },
    {
      "id": 6,
      "prompt": "Build an enterprise token system with semantic text, borders, typography, and custom collections.",
      "expected_output": "The generated artifact preserves literal token path identities from the references, validates alias targets against emitted JSON paths, and applies correct scopes to emitted semantic tokens. Text paths resolve to TEXT_FILL, border paths to STROKE, icon paths to SHAPE_FILL plus STROKE, and typography numeric paths to FONT_SIZE, LINE_HEIGHT, and LETTER_SPACING.",
      "files": [
        "scripts/validate_generator_core.py"
      ],
      "assertions": [
        {
          "name": "preserves-literal-path-identity",
          "description": "Reference paths such as link-hover, on-brand, on-surface-variant, lineHeight, letterSpacing, borderWidth, minWidth, and maxWidth are preserved without mutation"
        },
        {
          "name": "validates-emitted-artifact-not-just-registry",
          "description": "Alias validation checks emitted JSON paths, not only internal registry keys"
        },
        {
          "name": "semantic-scope-families-correct",
          "description": "Semantic text, border, icon, shadow color, and typography numeric scope families are emitted with their correct Figma scopes"
        }
      ]
    }
  ]
}
